# Video Folders

- `raw` stores original phone clips like `.mov`
- `web` stores browser-ready clips like `.mp4`

Suggested web filenames:

- `hero-loop.mp4`
- `salon-loop.mp4`
- `detail-loop.mp4`

When new web videos are ready, Codex can wire them into the homepage.
